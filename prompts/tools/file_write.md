<!--
name: "Tool: file_write"
description: "Create a file, or replace one entirely."
-->
Create a file, or replace one entirely. It overwrites without warning, so read an
existing file first and use file_edit instead when you only mean to change part of it.
Creating a file the user did not ask for is clutter they have to find later.
