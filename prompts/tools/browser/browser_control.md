<!--
name: "Tool: browser_control"
description: "Control the selected browser task: pause, private (ask the user for login in the website), resume (only after the user is done), stop, close, or show (only when the user asks)."
-->
Control the selected browser task: pause, private (ask the user for login in the
website), resume (only after the user is done), stop, close, or show (only when the user
asks). Use the latest revision. After resume, read a fresh observation before
interacting.
